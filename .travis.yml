language: node_js 

services:
  - postgresql
install: 
  - npm install
  - npm install --production
script:
  - npm run serve

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master